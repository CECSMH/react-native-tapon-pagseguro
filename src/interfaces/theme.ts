

export type ThemeSettings = {
    /**
     * Define a cor do texto da barra de ferramentas (toolbar).
     */
    toolbar_text_color?: string
    /**
     * Define a cor de fundo da barra de ferramentas (toolbar).
     */
    toolbar_color?: string
    /**
     * Define a cor da barra de status do dispositivo.
     */
    status_bar_color?: string
    /**
     * Define a cor de fundo dos botões de ação principais.
     */
    button_background_color?: string
    /**
     * 	Define a cor do texto dos botões de ação principais.
     */
    button_text_color?: string
}
