///
/// ErrorCode.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::taponpagseguro {

  /**
   * An enum which can be represented as a JavaScript enum (ErrorCode).
   */
  enum class ErrorCode {
    INVALID_RESULT      SWIFT_NAME(invalidResult) = 0,
    INVALID_PARAM      SWIFT_NAME(invalidParam) = 1,
    UNEXPECTED_ERROR      SWIFT_NAME(unexpectedError) = 2,
    OPERATION_CANCELED      SWIFT_NAME(operationCanceled) = 3,
    SERVICE_UNAVAILABLE      SWIFT_NAME(serviceUnavailable) = 4,
  } CLOSED_ENUM;

} // namespace margelo::nitro::taponpagseguro

namespace margelo::nitro {

  // C++ ErrorCode <> JS ErrorCode (enum)
  template <>
  struct JSIConverter<margelo::nitro::taponpagseguro::ErrorCode> final {
    static inline margelo::nitro::taponpagseguro::ErrorCode fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<margelo::nitro::taponpagseguro::ErrorCode>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::taponpagseguro::ErrorCode arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double number = value.getNumber();
      int integer = static_cast<int>(number);
      if (number != integer) {
        // The integer is not the same value as the double - we truncated floating points.
        // Enums are all integers, so the input floating point number is obviously invalid.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 4;
    }
  };

} // namespace margelo::nitro
