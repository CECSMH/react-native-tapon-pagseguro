///
/// HybridTaponPagseguroSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `SettingData` to properly resolve imports.
namespace margelo::nitro::taponpagseguro { struct SettingData; }
// Forward declaration of `CustomError` to properly resolve imports.
namespace margelo::nitro::taponpagseguro { struct CustomError; }
// Forward declaration of `ThemeSettings` to properly resolve imports.
namespace margelo::nitro::taponpagseguro { struct ThemeSettings; }
// Forward declaration of `PaymentResult` to properly resolve imports.
namespace margelo::nitro::taponpagseguro { struct PaymentResult; }

#include "SettingData.hpp"
#include "CustomError.hpp"
#include <optional>
#include "ThemeSettings.hpp"
#include <variant>
#include <NitroModules/Promise.hpp>
#include "PaymentResult.hpp"
#include <string>

namespace margelo::nitro::taponpagseguro {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `TaponPagseguro`
   * Inherit this class to create instances of `HybridTaponPagseguroSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridTaponPagseguro: public HybridTaponPagseguroSpec {
   * public:
   *   HybridTaponPagseguro(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridTaponPagseguroSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridTaponPagseguroSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridTaponPagseguroSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void initialize(const SettingData& data) = 0;
      virtual std::optional<CustomError> setTheme(const ThemeSettings& data) = 0;
      virtual bool isPackageAvailable() = 0;
      virtual bool isNfcEnabled() = 0;
      virtual bool isNfcSupported() = 0;
      virtual bool isExtensionSupported() = 0;
      virtual void openPackageOnPlaystore() = 0;
      virtual std::shared_ptr<Promise<std::variant<bool, CustomError>>> requestLogin() = 0;
      virtual std::shared_ptr<Promise<std::variant<bool, CustomError>>> requestLogout() = 0;
      virtual std::shared_ptr<Promise<std::variant<CustomError, PaymentResult>>> requestPayment(double amount) = 0;
      virtual std::shared_ptr<Promise<std::variant<bool, CustomError>>> requestRefund(const std::string& transaction_code, double amount) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "TaponPagseguro";
  };

} // namespace margelo::nitro::taponpagseguro
